'use client'

import { Box } from '@mui/material'

type Props = {}

export default function SearchBar({}: Props) {
  return (
    <Box
      display="flex"
      alignItems="center"
      borderRadius="4px"
      bgcolor="white"
    ></Box>
  )
}
